import { FilterSelectDirective } from './filter-select.directive';

describe('FilterSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterSelectDirective();
    expect(directive).toBeTruthy();
  });
});
